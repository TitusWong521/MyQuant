1. 在打包之前务必找到第三方库的包，把包复制到到跟主程序同目录下
2. 使用如下命令打包，否则会打包失败或者即使打包成功，程序也会闪退。
   pyinstaller -F -i bitcoin.ico BinanceAutoTrader.py
   -F 表示生成单个可执行文件
   -w 表示去掉控制台窗口，这在GUI界面时非常有用。不过如果是命令行程序的话那就把这个选项删除吧！
   -p 表示你自己自定义需要加载的类路径，一般情况下用不到
   -i 表示可执行文件的图标
